import * as React from 'react';
import { MemoryMapV2 } from './memory-map-v2';

// The /docs overview diagram: one memory, three ways in (horizontal flow).

export function MemoryMapDiagram(): React.JSX.Element {
  return <MemoryMapV2 />;
}
